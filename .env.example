# docker-compose configuration (copy to .env and adjust as needed)

# PostgreSQL
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=job_posting
DB_PORT=5432

# Exposed host ports
SERVER_PORT=4000
CLIENT_PORT=5173

# Allowed CORS origin for the API
CORS_ORIGIN=http://localhost:5173
